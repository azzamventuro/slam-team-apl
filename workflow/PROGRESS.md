# PROGRESS — laporan pengerjaan modul

Tracker per-prompt untuk build SLAM Team. Satu baris = satu prompt yang di-paste ke Claude Code (API atau APP). Urutan = build order (`EXECUTION.md` §3). Tandai selesai setiap kali sebuah prompt tuntas + terverifikasi + ter-commit.

**Progress: 35 / 49 selesai** · 🟡 0 proses · ⬜ 14 belum

> ⚠️ **#32 `16-jadwal` APP (kalender/form) di-SKIP** — belum dibangun (urutan sempat meloncat ke penugasan). `pages/jadwal/` saat ini hanya berisi panel penugasan (#34). Kerjakan #32 sebelum menganggap Fase 3 tuntas.

Status: ⬜ Belum · 🟡 Proses · ✅ Selesai · ⛔ Terblokir

### Cara update (perintah ke AI)
Saat sebuah prompt selesai, minta AI: **"update PROGRESS: `<modul>` `<api|app>` selesai, commit `<hash>`"**. AI akan:
1. Ubah **Status** baris itu ⬜ → ✅ (atau 🟡 saat sedang dikerjakan).
2. Isi **Tanggal** (YYYY-MM-DD), **Commit** (hash pendek), dan **Catatan** bila ada.
3. Perbarui angka **Progress: N / 49** di atas.
4. Tambahkan satu baris ke **Riwayat** di bawah.
5. (Opsional) tandai **V** modul terkait di tabel `00-general-tasks.md`.

Aturan tetap: API dulu, baru APP. Verifikasi = checklist prompt + `make build && go vet ./...` (API) / `npm run build:local` (APP) + satu round-trip **form → API → DB** (row masuk Postgres, envelope benar, role terlarang → 403). Commit per prompt.

---

## Fase 0 — Fondasi

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 1 | 01-foundation | API | Opus · high | ✅ | 2026-09-09 | `46e69f6` | envelope+`apperr`+`FromError`, `model.Audit`, `pagination`, tzdata, CORS config, `slamctl` + migrator |
| 2 | 01-foundation | APP | Opus · high | ✅ | 2026-09-09 | `46e69f6` | `ApiService` + `Page<T>`, interceptor 401/403, design tokens (dark + merah), i18n `COMMON.*`/`VALIDATION.*` |
| 3 | 03-pengaturan-log | API | Sonnet · high | ✅ | 2026-09-09 | `c90d083` | migrasi `0002` + seeder 24 baris idempoten, `shared/audit` (writer lintas modul), gate `RequireAdmin` + `is_terkunci` SA-only di service |
| 4 | 03-pengaturan-log | APP | Sonnet · high | ✅ | 2026-09-09 | `62f1a0d` | halaman self-generating per `tipe_nilai`, `adminGuard` meniru `RequireAdmin` (belum ada `pengaturan.*`), `User.role` ditambah (opsional — auth Fase 0 belum menerbitkan klaim peran) |
| 5 | 02-file-management | API | Opus · high | ✅ | 2026-09-09 | `01d98d9` | migrasi `0003` identik `.dbml`; `imaging` (murni Go, WebP ditunda → JPEG); varian px dari `mst_pengaturan`; serve ber-izin via `JWTOptional`; seam `perm.Require` siap untuk 05 |
| 6 | 02-file-management | APP | Opus · high | ✅ | 2026-09-09 | `e0a52a5` | `FileService` di atas `ApiService`; `<app-file-upload>` (dropzone → emit `{id,uuid}`) + `<app-secure-image>` (blob privat, revoke object URL); `UploadResp` belum punya `id` ⇒ `FileRef.id` masih undefined |
| 7 | 04-auth-session | API | Opus · high | ✅ | 2026-09-12 | `666da22` | migrasi `0013_sesi_login`; `domain.User` = skema `users` asli (tanpa `name` — `nama` join `anggota`); login username/email satu query, lockout 5× → 15m; refresh opaque + rotasi, logout revoke; `jwt.Claims.AnggotaID`; `/me` + ringkasan anggota; fix `GetPermVersion` (selalu 0) |
| 8 | 04-auth-session | APP | Opus · high | ✅ | 2026-09-12 | `2a544cf` | login `identifier` (username/email) + `slam_refresh`; refresh senyap sekali di `errorInterceptor` lalu replay; logout revoke + clear + redirect; hydrate `/auth/me` saat boot; `PermissionService`/guard/directive dipakai apa adanya. E2E menunggu handler refresh/logout di #7 |

## Fase 1 — Hak akses dinamis

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 9 | 05-hak-akses | API | Opus · xhigh ★ | ✅ | 2026-09-10 | `6d46005` | RBAC penuh: role CRUD, permission matrix, `middleware.RequirePermission`, seeder 19 modul + 3 file permissions |
| 10 | 05-hak-akses | APP | Opus · xhigh ★ | ✅ | 2026-09-10 | `6d46005` | RBAC grid, role-form, role-matrix, `adminGuard`, `PermissionService`, `HasPermissionDirective` |

## Fase 2 — Anggota + master data

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 11 | 08-instansi | API | Sonnet · high | ✅ | 2026-09-10 | `6d46005` | migrasi `0005_mst_instansi`, CRUD lengkap, logo upload via file-management |
| 12 | 08-instansi | APP | Sonnet · high | ✅ | 2026-09-10 | `6d46005` | list + form + pagination + search + status filter + logo lazy-load |
| 13 | 06-anggota | API | Sonnet · high | ✅ | 2026-09-10 | `273d305` | migrasi `0006_anggota` (+ `mst_wilayah`), 5 CRUD endpoints, FK validators, audit log, soft-delete guards (active KTA/user account) |
| 14 | 06-anggota | APP | Sonnet · high | ✅ | 2026-09-10 | `273d305` | list (search+jenis/status filter+pagination), form (reactive+3 file upload+preview), detail (blob image load), i18n `ANGGOTA.*` |
| 15 | 07-user-management | API | Opus · high | ✅ | 2026-09-10 | `273d305` | anti-eskalasi, 7 endpoints, anti-escalation rules, dual-band handler |
| 16 | 07-user-management | APP | Opus · high | ✅ | 2026-09-11 | `98b108f` | 3 surfaces (admin/moderator/user), list (signal, search, status filter, pagination), form (anggota picker, role select with anti-escalation, buka kunci), routing + i18n IND/ENG |
| 17 | 09-unit | API | Sonnet · high | ✅ | 2026-09-10 | `273d305` | migrasi `0007_unit`, 6 endpoints CRUD, approval flow via PUT, scope enforcement, anggota tersedia, foto UUID→file_id resolve |
| 18 | 09-unit | APP | Sonnet · high | ✅ | 2026-09-10 | `273d305` | unit-list (signal, filter, pagination), unit-form (anggota picker, spec fields, foto upload, approval toggle), routing, i18n IND/ENG |
| 19 | 10-prestasi | API | Sonnet · high | ✅ | 2026-09-11 | `273d305` | migrasi 0008, CRUD 6 endpoints, tingkat enum, anggota FK, audit |
| 20 | 10-prestasi | APP | Sonnet · high | ✅ | 2026-09-11 | `273d305` | list + form + tingkat filter, foto/flyer upload, i18n |
| 21 | 11-inorga | API | Sonnet · high | ✅ | 2026-09-11 | `273d305` | migrasi 0009, CRUD, logo/banner/SK upload, status toggle |
| 22 | 11-inorga | APP | Sonnet · high | ✅ | 2026-09-11 | `273d305` | list + form (CKEditor-style textarea), 3 file uploads, i18n |
| 23 | 12-medsos | API | Sonnet · high | ✅ | 2026-09-11 | `273d305` | migrasi 0010, CRUD, anggota FK, kode unique per anggota |
| 24 | 12-medsos | APP | Sonnet · high | ✅ | 2026-09-11 | `273d305` | list + form, jenis filter, anggota picker, i18n |
| 25 | 13-dokumen | API | Sonnet · high | ✅ | 2026-09-11 | `273d305` | migrasi 0011, CRUD, file upload (kategori=dokumen), download |
| 26 | 13-dokumen | APP | Sonnet · high | ✅ | 2026-09-11 | `273d305` | list + form + file upload + download, reff_type filter, i18n |
| 27 | 14-profile-club | API | Sonnet · high | ✅ | 2026-09-11 | `273d305` | singleton guard (409), 6 endpoints, public profil endpoint, 3 file refs |
| 28 | 14-profile-club | APP | Sonnet · high | ✅ | 2026-09-11 | `273d305` | singleton form (auto create/edit), 3 image uploads, i18n |

## Fase 3 — Lokasi + jadwal

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 29 | 15-lokasi | API | Sonnet · high | ✅ | 2026-09-12 | `9b8221d` | migrasi 0014, geofence CHECK + timezone IANA, delete guard jadwal = seam TODO(16-jadwal) |
| 30 | 15-lokasi | APP | Sonnet · high | ✅ | 2026-09-13 | `1be1e6a` | Leaflet 1.9.4+OSM (dependency baru pertama); E2E berautentikasi belum — kredensial SA lokal tak diketahui |
| 31 | 16-jadwal | API | Opus · xhigh ★ | ✅ | 2026-09-13 | `ed849de` | migrasi 0015 (`jadwal`+`jadwal_sesi`, FK `kegiatan_id` ditunda), generator sesi per pola + lokal→UTC via `jadwal.timezone`, idempoten ON CONFLICT, batalkan sesi 409-guard; seam lokasi ditutup (hapus lokasi terpakai → 409, `jumlah_jadwal` nyata); fix `validator` kunci errors = nama JSON |
| 32 | 16-jadwal | APP | Opus · xhigh ★ | ⬜ |  |  | kalender sadar timezone |

## Fase 4 — Penugasan + notifikasi

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 33 | 17-penugasan-notifikasi | API | Opus · high | ✅ | 2026-09-13 | `387508b` | migrasi 0016 (`jadwal_peserta`+`notifikasi`), `wajib_absen` auto-false tanpa akun, fan-out 1 baris/penerima, `jml_ditugaskan` di-recompute, `notifikasi.Service` reusable (Kirim/KirimKeAnggota/WithTx) |
| 34 | 17-penugasan-notifikasi | APP | Opus · high | ✅ | 2026-09-13 | `c9ca05a` | `peserta-panel` (single + bulk by-instansi → `anggota_ids[]`), `respon` Terima/Tolak baris sendiri (bearer), `notif-bell` di topbar lama (badge poll 60s + refresh pasca-aksi); `/jadwal/:id` → redirect ke panel sampai #32 ada. E2E lulus dengan data uji `claude_*` |

## Fase 5 — Absensi + izin

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 35 | 19-izin | API | Opus · high | ✅ | 2026-09-13 | `b56f8ad` | migrasi `0017_absensi_izin`, `POST/GET /izin` + `PATCH /izin/{id}/approve\|tolak`, pengaju = `claims.AnggotaID`, cakupan `milik_sendiri` di service, notifikasi `izin_disetujui`/`izin_ditolak` satu transaksi, seam `TODO(18-absensi)` |
| 36 | 19-izin | APP | Opus · high | ✅ | 2026-09-13 | `6ad0e4d` | `pages/izin/` — form (picker jadwal→sesi, native time hanya `pulang_cepat`, lampiran privat via `<app-file-upload>`), list ber-cakupan, antrian approval (Setujui/Tolak+catatan). Rute `/izin` (read/create/approve). i18n `IZIN.*` + `FILE.*`/`CHOOSE_FILE` IND↔ENG. `build:local` ok; `npm test` 44/44 |
| 37 | 18-absensi | API | Opus · xhigh ★ | ⬜ |  |  | server-time authority |
| 38 | 18-absensi | APP | Opus · xhigh ★ | ⬜ |  |  | kamera + GPS |

## Fase 6 — KTA / NRA / QR

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 39 | 20-kta-nra-qr | API | Opus · xhigh ★ | ⬜ |  |  | NRA sequence + PNG |
| 40 | 20-kta-nra-qr | APP | Opus · xhigh ★ | ⬜ |  |  | halaman QR publik |

## Fase 7 — Laporan / rekap

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 41 | 21-laporan-rekap | API | Opus · high | ⬜ |  |  | export xlsx/pdf |
| 42 | 21-laporan-rekap | APP | Opus · high | ⬜ |  |  |  |

## Fase 8 — Landing + konten publik

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 43 | 22-kegiatan | API | Sonnet · high | ⬜ |  |  |  |
| 44 | 22-kegiatan | APP | Sonnet · high | ⬜ |  |  |  |
| 45 | 23-artikel | API | Sonnet · high | ⬜ |  |  |  |
| 46 | 23-artikel | APP | Sonnet · high | ⬜ |  |  |  |
| 47 | 24-landing-publik | API | Sonnet · high | ⬜ |  |  | endpoint publik |
| 48 | 24-landing-publik | APP | Sonnet · high | ⬜ |  |  | rute di luar authGuard |

## Fase 9 — PWA

| # | Modul | Bagian | Model · Effort | Status | Tanggal | Commit | Catatan |
|---|-------|--------|----------------|--------|---------|--------|---------|
| 49 | 25-pwa | APP | Sonnet · high | ⬜ |  |  | manifest + SW (tanpa API) |

---

## Riwayat

- **2026-09-13** — #36 `19-izin` APP selesai (Opus · high) — commit `6ad0e4d`. Folder baru `src/app/pages/izin/` mengikuti pola `lokasi`/`prestasi` (bukan dibangun dari nol); pakai ulang `ApiService`, `PermissionService`/`permissionGuard`, `HasPermissionDirective`, `<app-file-upload>`, `ToastService`, `SlamIconComponent` apa adanya. `izin.model.ts` cermin `dto.IzinResp`/`CreateIzinReq`/`TolakReq`/`ListIzinQuery` yang hidup di kode Go; `izin.service.ts` tipis + dua baca jadwal (`GET /jadwal` per_page 100, `GET /jadwal/:id/sesi`) untuk **picker sesi dua langkah** (pilih jadwal → sesinya) karena tak ada endpoint "penugasan-saya"; sesi `dibatalkan` disaring (API tolak 422 juga). **`izin-form`**: `jadwal_id`→`sesi_id`→`jenis`(select)→`alasan`(textarea, maxlength 2000)→`waktu_pulang_diminta` (`<input type=time>` **muncul & wajib hanya bila `jenis=pulang_cepat`**, validator ditambah/dilepas reaktif; untuk jenis lain field dibuang dari body — cermin aturan lintas-field service)→lampiran opsional (`<app-file-upload kategori=dokumen [isPublik]=false>`, PDF/gambar, privat). 422 dipetakan inline per kontrol, `lampiran_file_id` (tanpa kontrol) → toast, 409 → `IZIN.DUPLICATE`, 403 → `IZIN.NO_ANGGOTA`; akun tanpa `anggota_id` diblok di depan (banner + submit disabled) sebelum round-trip. **`izin-list`**: signal list ber-cakupan (User = miliknya, Admin/Mod = semua — backend yang menegakkan; kolom **Anggota** hanya muncul saat `izin.approve`), filter `status`+`jenis`, paginasi; alasan di-clamp 2 baris (`title` penuh), lampiran dibuka via blob (`FileService.imageUrl(...,'original')` + `window.open`, revoke setelah 60s) karena `<a href>` tak bawa Bearer. **`izin-approval`**: antrian `menunggu` (terlama dulu) kartu who|what|decide, **Setujui**/**Tolak** di balik `*hasPermission='izin.approve'` (+guard rute); Tolak membuka textarea `catatan_peninjau` inline (wajib, maxlength 2000); baris diproses dibuang lokal, hanya refetch bila halaman kosong; **409** (diproses peninjau lain) → `warning` + reload. `izin-parts.ts` = chip status (info/success/danger) + jenis (info) + tombol lampiran, **status tak pernah lewat warna saja** — dipasangkan glyph+label (design-tokens §11). Rute `/izin` (`izin.read`) anak `''`=list, `new`=form (`izin.create`), `approval` (`izin.approve`); **tanpa** entri menu hard-code — `izin` modul sidebar ter-seed (grup Operasional, `GLYPHS.izin='clock'` sudah ada). i18n `IZIN.*` + kunci `FILE.*`/`COMMON.CHOOSE_FILE` yang **hilang** (dipakai `<app-file-upload>` sejak #2 tapi tak pernah ditambahkan ke i18n) — IND↔ENG diverifikasi paritas kunci penuh. Taste: token SLAM (gelap, aksen/fokus merah, heading UPPERCASE, target 44px, `prefers-reduced-motion`). **Verifikasi:** `npm run build:local` lolos (AOT + strict template type-check); `npm test` **44/44**. **Belum diverifikasi:** round-trip berautentikasi di app sungguhan (ajukan izin +lampiran +pulang_cepat, list cakupan, approve/tolak, gating 403 User) — login butuh kredensial yang tidak dimiliki sesi ini (klasifikator auto-mode memblok pencarian/percobaan kata sandi), dan `slamctl create-superadmin` menolak akun kedua by design. Data uji tertinggal dari #34 (`claude_sa` id 2 SA, `claude_user` id 3 User, jadwal id 1 `CLAUDE-TEST` + 3 sesi) tersedia untuk operator yang punya kredensialnya.
- **2026-09-13** — #34 `17-penugasan-notifikasi` APP selesai (Opus · high) — commit `c9ca05a`. **Prasyarat #32 (16-jadwal APP) belum ada** — folder `pages/jadwal/` dibuat hanya berisi penugasan: `peserta-panel` di `/jadwal/:id/peserta` (guard `jadwal.read`), plus redirect sementara `/jadwal/:id` & `/jadwal/:id/sesi/:sesiId` → panel karena `notifikasi.route` API menunjuk ke sana; #32 tinggal mengganti redirect itu dengan halaman detail/kalender. Panel: header identitas jadwal (`GET /jadwal/:id` + `/sesi`) + 4 ubin ringkasan status; form tugaskan (gate `*hasPermission='jadwal.assign'`) dua mode — **Tunggal** (picker anggota debounce 250ms, chip terpilih, opsi sesi/peran/wajib_absen/keterangan) dan **Massal** (checklist berhalaman 50, filter instansi + cari, tombol *Pilih semua anggota instansi* memakai `expand` menelusuri semua halaman `/anggota?instansi_id=` lalu mengisi `anggota_ids[]`, batas 500 sesuai DTO; anggota yang sudah ada dinonaktifkan dengan lencana). Hasil bulk `{ditugaskan, dilewati}` di-toast lewat `translate.instant` berparameter. Kolom `wajib_absen` **read-only** (API pemilik flag) dengan catatan *otomatis nonaktif — anggota tanpa akun* saat `!anggota_berakun`. `respon.ts` = `<app-peserta-respon>` Terima/Tolak, dipasang hanya pada baris yang `anggota_id`-nya = `user().anggota_id` (baris disorot `--slam-primary-soft` + lencana *Anda*), tanpa gate izin; emit baris hasil → list dipatch tanpa refetch. `notif-bell` ditambahkan ke `layouts/topbar/` yang ada (bukan topbar baru): `effect` pada `auth.isLoggedIn()` memulai/menghentikan `setInterval` 60s ke `/jumlah-belum-dibaca`, refresh ulang setelah buka/mark-read/mark-all, dropdown 15 notifikasi terbaru, klik → mark-read optimistis + `router.navigateByUrl(route)`, tutup via klik luar/Escape; warna backend (`info/sukses/peringatan/bahaya`) → garis kiri + ikon, bukan warna saja. Services tipis `penugasan.service.ts` (pages/jadwal) & `core/services/notifikasi.service.ts` di atas `ApiService`; model mencerminkan `domain`/`dto` Go. Inner `catchError` di setiap `switchMap` picker agar 500 API tidak mematikan stream. i18n `PESERTA.*` + `NOTIF.*` IND/ENG sinkron (535 kunci). **Verifikasi:** `npm run build:local` lolos; round-trip curl: assign anggota tanpa akun → `wajib_absen=false`, bulk `[dup,A2,A3]` → `{2,1}`, User → 403 assign/remove & 403 respon baris orang lain, respon sendiri → `diterima`, notif list/count/baca/baca-semua; UI di Chrome (SA & User): single, bulk (toast `Ditugaskan: 0, dilewati: 1`), hapus, Terima/Tolak, lonceng badge 3→2→0, klik notifikasi → panel, ENG tanpa kunci mentah. **Temuan di luar scope (API, sudah ada sebelumnya):** `POST /anggota` 500 (GORM menyisipkan kolom proyeksi `instansi_nama`), `GET /instansi` 500 (`is_deleted` ambigu setelah join logo), `GET /modul` 404 (sidebar kosong), dan binary `bin/slam-team-api.exe` tertinggal dari #33 (dibangun ulang saat uji). **Data uji** tertinggal di DB lokal: users `claude_sa` (id 2, SA) & `claude_user` (id 3, User), anggota id 2–5 `Claude Test *`, jadwal id 1 `CLAUDE-TEST`, baris `jadwal_peserta` + `notifikasi` terkait — hapus bila tidak diperlukan.
- **2026-09-13** — #30 `15-lokasi` APP selesai (Sonnet · high) — commit `1be1e6a`. Mengikuti struktur `instansi`/`unit` (list + form + routing, bukan dibangun dari nol); pakai ulang `ApiService`, `PermissionService`/`permissionGuard`, `HasPermissionDirective`, `<app-file-upload>`/`<app-secure-image>` apa adanya. `lokasi.model.ts` cermin persis `dto.LokasiResp`/`CreateLokasiReq` yang hidup di kode (bukan hanya doc modul); `listTimezones()` pakai `Intl.supportedValuesOf('timeZone')` dengan fallback daftar IANA condong Indonesia. **Peta: Leaflet 1.9.4 + `@types/leaflet` + tile OpenStreetMap, tanpa API key** — dependency baru pertama di app, dicatat versi+keputusan di `99-alignment-report.md` §4/§5 (CSS didaftarkan global lewat `angular.json`, bukan `@import` SCSS komponen, karena Leaflet menyuntik DOM lewat API DOM biasa di luar `ViewEncapsulation`; ikon marker `L.divIcon` dengan style inline, bukan PNG bawaan yang rusak di bawah esbuild). "Lingkaran yang bisa diseret" = marker draggable asli (dukungan native Leaflet) + circle yang selalu mengikuti posisinya — circle sendiri tidak bisa di-drag tanpa plugin (`Leaflet.Path.Drag`) yang dianggap tidak sepadan hanya untuk kapabilitas yang sudah didapat gratis dari marker; radius tetap dua-arah lewat `<input type=range>` + `<input type=number>` yang berbagi satu `FormControl`, keduanya memicu `circle.setRadius()`. Warna SLAM red ditulis sebagai literal hex di opsi Leaflet (`setAttribute` SVG mentah, di luar cascade tempat `var()` di-resolve) — satu-satunya penyimpangan sengaja dari aturan "jangan hardcode hex", beralasan teknis. Kategori foto lokasi: `banner` (bukan `'foto'` yang dipakai beberapa modul lama dan sebetulnya tidak ada di `oneof` backend). List: filter `q`+`is_aktif`, hapus diblok klien saat `jumlah_jadwal>0` dan 409 dari server dipetakan ke `LOKASI.DELETE_IN_USE`, bukan pesan generik. Routing `'new'`/`':id/edit'` (persis nama path di prompt, beda dari konvensi `'create'` modul lain) masing-masing bawa `permissionGuard`+`data.permission` sendiri (`lokasi.create`/`lokasi.update`) — selangkah lebih ketat dari modul sebelah yang hanya menjaga di level list. **Perbaikan aditif lintas-modul (di luar lingkup lokasi tapi ditemukan saat membangunnya):** `dto.UploadResp` (`02-file-management`) ditambah `ID int64` (diisi dari `f.ID`) karena `POST /files` tidak pernah mengembalikan id numerik `mst_file`, padahal DTO `lokasi` (dan hampir semua modul lain: instansi/anggota/prestasi/inorga/profile_club) butuh `*_file_id bigint`, bukan uuid — tanpa ini beberapa halaman yang sudah "selesai" diam-diam mengirim `undefined` untuk FK foto-nya. `go build`/`go vet`/`go test ./internal/modules/core/file/...` bersih; perubahan murni aditif, tidak menyentuh modul lain yang sudah memakainya. **Verifikasi:** `npm run build:local` lolos; `npm test` **44/44** tidak berubah (halaman CRUD per-modul di app ini memang tidak punya spec file sendiri — konsisten, bukan celah baru). Logika peta (klik → titik, drag marker → titik, `setRadius` → lingkaran membesar) diverifikasi di Chrome sungguhan lewat harness Leaflet statis di luar repo yang memanggil API persis sama — dikonfirmasi log + tangkapan layar, lalu dibuang. **Belum diverifikasi:** uji berautentikasi di app sungguhan (create/edit/delete lewat UI, gating 403 Mod/User, unggah foto sungguhan) — `slamteam_db` lokal sudah punya satu super admin yang kredensialnya tidak diketahui sesi ini, `slamctl create-superadmin` menolak membuat yang kedua by design, dan menulis akun/baris uji langsung ke database bersama yang sedang dipakai sesi-sesi lain (jadwal/penugasan-notifikasi API selesai hari yang sama) dinilai berisiko tanpa izin eksplisit — dicatat di `99-alignment-report.md` §4 untuk operator yang punya kredensialnya. i18n `LOKASI.*`, IND/ENG sinkron (476 kunci).

- **2026-09-13** — #35 `19-izin` API selesai (Opus · high) — commit `b56f8ad`. Modul baru `internal/modules/core/izin/` (`Initialize(db, jwtMgr, permGuard, auditor, notifModule.Service())`, didaftarkan di `router.go` setelah `penugasan`). Rute: `POST /izin` (`izin.create`), `GET /izin` (`izin.read`, cakupan), `PATCH /izin/{id}/approve` + `PATCH /izin/{id}/tolak` (`izin.approve`). Migrasi **`0017_absensi_izin`** persis `.dbml`: enum `jenis_izin`/`status_izin` dari `0001`; FK `sesi_id→jadwal_sesi` **RESTRICT**, `jadwal_id→jadwal`, `anggota_id→anggota` **RESTRICT**, `lampiran_file_id→mst_file`, `diproses_oleh→users`, audit `*_by→users` SET NULL; index `(sesi_id,anggota_id)` + `(status,created_at)`; `up`→v17, `down 1`→v16 (tabel hilang), `up`→v17 bersih. `absensi.izin_id` **tidak** dibuat di sini (milik migrasi absensi). **Aturan:** body `POST /izin` tidak punya `anggota_id` — pengaju **selalu** `claims.AnggotaID` (akun tanpa anggota → 403), sehingga cakupan `izin.create` tidak pernah melebar; `jadwal_id` disalin dari sesi; `waktu_pulang_diminta` (`HH:MM`/`HH:MM:SS`, disimpan `time`) **wajib** untuk `pulang_cepat` dan **ditolak** (422) untuk jenis lain; `alasan` wajib; `sesi_id` tak ada / `dibatalkan` → 422 field; `lampiran_file_id` divalidasi ke `mst_file` non-deleted (422); satu pengajuan `menunggu`/`disetujui` per (sesi, anggota) → **409**, yang `ditolak` boleh ajukan ulang. `GET /izin` paginated (`pagination.New`), filter `status`/`sesi_id`/`jadwal_id`/`jenis`/`q` (nama/no_induk), `sort` whitelist `created_at|status|jenis|diproses_pada` (default terbaru dulu); cakupan dibaca `GetCakupan("izin.read")`: `milik_sendiri` → `anggota_id = claims.AnggotaID` (akun tanpa anggota → kosong), `semua`/SA (guard tidak set cakupan) → semua. Approve/tolak **one-shot**: `UPDATE … WHERE status='menunggu'` (0 baris → 409, aman terhadap dua peninjau bersamaan); set `diproses_oleh/pada` (+`catatan_peninjau` wajib saat tolak, 422); notifikasi `izin_disetujui` (sukses) / `izin_ditolak` (peringatan, isi memuat catatan) ke pengaju via `KirimKeAnggota` **dalam transaksi yang sama** (`WithTx`), route `/izin`, `reff_type=absensi_izin`. Approve jenis izin/sakit/dinas juga men-set `jadwal_peserta.status_tugas='izin'` **hanya pada baris penugasan spesifik sesi itu** (baris `sesi_id NULL` = semua sesi dibiarkan — seam #3 modul 17 ditutup sebagian); `pulang_cepat` tidak menyentuh roster. `log_aktivitas` modul `izin`: create / approve / tolak. Respons `IzinResp` memuat proyeksi `anggota_nama`, `no_induk`, `sesi_tanggal`, `sesi_status`, `jadwal_nama/kode`, `diproses_oleh_nama`, `lampiran{file_id,uuid,url=/api/v1/files/{uuid}/original}`. **Seam sengaja:** `// TODO(18-absensi)` di `service.Approve` — tulis/perbarui baris `absensi` (`izin_id`, `status_kehadiran`=jenis) agar anggota tidak alfa; tabel `absensi` belum ada dan tidak dirujuk. Tidak ada `GET /izin/{id}` (di luar §9). **Verifikasi:** `go build ./... && go vet ./... && go test ./...` bersih; DB sekali-pakai `slamteam_izintest` (seed + `create-superadmin` + Admin/User/User/akun-tanpa-anggota via SQL, jadwal harian 1–3 Okt → 3 sesi, Citra ditugaskan seluruh jadwal, Dedi sesi 2): 422 (alasan kosong, waktu untuk non-pulang_cepat, pulang_cepat tanpa waktu, jam `25:99`, jenis salah, sesi tak ada, sesi dibatalkan, lampiran tak ada, filter `status` salah), 403 (akun tanpa anggota; User approve/tolak), 401 tanpa token; create 201 ×6 (User/Admin), duplikat aktif → 409; list: Citra 3 (hanya miliknya), Dedi 2, Admin 6, SA 6, filter `status+sesi_id`/`jenis+sort`/`q`; approve → `disetujui` + notifikasi `izin_disetujui`, approve/tolak ulang → 409, tolak tanpa catatan → 422, tolak → `ditolak` + `izin_ditolak` (catatan ikut), 404/400 id; roster Dedi sesi 2 → `izin`, baris Citra `sesi_id NULL` tetap `ditugaskan`; tolak lalu ajukan ulang → 201; `log_aktivitas` 12 baris; DB di-drop. Unggah lampiran sungguhan **tidak** sempat diuji end-to-end (PDF dummy 21 byte ditolak file layer) — jalur negatif `mst_file` + join `lampiran_uuid` teruji. **Catatan commit:** perubahan pending `slam-team-app/` (17 APP lonceng notifikasi + halaman jadwal) dan `workflow/prompts/04-*` sengaja **tidak** ikut — commit di-scope ke `slam-team-api/`.
- **2026-09-13** — #33 `17-penugasan-notifikasi` API selesai (Opus · high) — commit `387508b`. Dua modul baru: `internal/modules/core/notifikasi/` (inbox in-app, `Initialize(db, jwtMgr)`, didaftarkan **sebelum** produsen) dan `internal/modules/core/penugasan/` (`Initialize(db, jwtMgr, permGuard, auditor, notifModule.Service())`), keduanya di `router.go` setelah `jadwal`. Rute: `GET /jadwal/{id}/peserta` (`jadwal.read`), `POST /jadwal/{id}/peserta`, `POST /jadwal/{id}/peserta/bulk`, `DELETE /jadwal/{id}/peserta/{anggotaId}` (`jadwal.assign`, sudah ter-seed Admin/Mod), `PATCH /peserta/{id}/respon` (bearer, hanya assignee: `claims.AnggotaID == row.anggota_id`, selain itu **403** — SA pun ditolak karena respon = tindakan pribadi), `GET /notifikasi`, `GET /notifikasi/jumlah-belum-dibaca` → `{jumlah}`, `PATCH /notifikasi/{id}/baca`, `PATCH /notifikasi/baca-semua` → `{ditandai}` (bearer, semua keyed `claims.UserID`; milik orang lain → 404, `user_id` tidak pernah diserialisasi). Migrasi **`0016_jadwal_peserta_notifikasi`** persis `.dbml`: enum `status_tugas`/`prioritas_notifikasi` dirujuk dari `0001`; `jadwal_peserta` triplet soft-delete (tanpa created/modified — ditambah `model.SoftDelete` di shared), index `(anggota_id,jadwal_id)` + `(jadwal_id,sesi_id)` (**non-unique** sesuai DBML — dedupe di aplikasi di bawah `SELECT … FOR UPDATE` baris jadwal), FK `jadwal_id→jadwal` RESTRICT, `sesi_id→jadwal_sesi`, `anggota_id→anggota` RESTRICT, `ditugaskan_oleh→users`, `deleted_by→users` SET NULL; `notifikasi` `uuid` default `gen_random_uuid()` UNIQUE, index `(user_id,is_dibaca,created_at)` + `(reff_type,reff_id)` + `kedaluwarsa_pada`, FK `user_id→users` **CASCADE**, `created_by→users` SET NULL. `down` drop keduanya, enum tetap; `up`→v16, `down 1`→v15 (tabel hilang), `up`→v16 bersih. **Aturan:** penugasan memakai `anggota_id` (bukan user); `wajib_absen` = nilai request (default true) **AND anggota berakun** (`users` non-deleted) — tanpa akun dipaksa `false` walau request `true`; `peran_peserta` oneof peserta/pelatih/panitia/pengawas (default peserta); `sesi_id` null = semua sesi, sesi harus milik jadwal (422) dan tidak `dibatalkan` (422); jadwal `dibatalkan`/`selesai` → 409; anggota tak ada → 422 field; single duplikat `(anggota_id,jadwal_id)` aktif → **409**, bulk duplikat → **dilewati** (`{ditugaskan, dilewati}` verbatim; id duplikat dalam request di-dedup; anggota tak ada → 422 tanpa menulis apa pun; cap 500 id). Insert + fan-out + counter dalam **satu transaksi** (`repo.Transaction` + `WithTx`). `jadwal_sesi.jml_ditugaskan` **di-recompute** per jadwal (baris sesi + baris null-sesi) setelah assign/bulk/remove, bukan increment — tahan remove/re-assign. Hapus = soft delete semua baris aktif anggota di jadwal itu (0 baris → 404), re-assign setelah hapus boleh. Respon set `status_tugas` + `direspon_pada` (+`keterangan`), boleh diubah ulang, jadwal batal/selesai → 409; penugas mendapat notifikasi `penugasan_direspon` (best effort). `log_aktivitas` modul `penugasan`: assign / assign_bulk / remove / respon. **Service notifikasi reusable** (`notifikasi/service.NotifikasiService`): `Kirim(ctx, userIDs, Pesan)` fan-out 1 baris/penerima (dedup, chunk 500, tipe+judul wajib, truncate varchar otomatis, warna default info, prioritas default normal), `KirimKeAnggota(ctx, anggotaIDs, Pesan)` resolve akun & buang yang tanpa akun, `UserIDsByAnggota`, `WithTx(tx)` untuk atomik dengan perubahan pemicu; konstanta tipe/warna di `notifikasi/domain`. Modul izin/absensi/kta tinggal menerima `notifModule.Service()` di `Initialize`. **Seam sengaja:** (1) filter bulk by-instansi/jenis (rancangan Bab 4.4) belum — body prompt hanya `anggota_ids[]`; (2) sesi yang di-generate **setelah** penugasan `sesi_id=null` mulai dengan `jml_ditugaskan=0` sampai ada assign/remove berikutnya pada jadwal itu (`SyncJmlDitugaskan` bisa dipanggil dari `generate-sesi` bila perlu); (3) `status_tugas=izin` bukan respon (di-set modul 19-izin); (4) job pembersih `kedaluwarsa_pada` belum ada. **Verifikasi:** `go build ./... && go vet ./... && go test ./...` bersih; DB dev `slamteam_db` tetap v1 DIRTY (tak disentuh) → DB sekali-pakai `slamteam_migtest` (disk C: penuh 0 byte — Go build cache 1,9 GB dibersihkan agar Postgres bisa membuat DB; `%LOCALAPPDATA%\Temp` masih ~12,5 GB sisa installer lama, tidak disentuh), seed rbac/instansi/pengaturan + `create-superadmin` + Mod/User/User + 2 anggota tanpa akun via SQL, jadwal harian 1–3 Okt → 3 sesi; **36/36** pemeriksaan HTTP lulus: User assign/bulk/remove → 403, User list → 200, tanpa token → 401; 422 (anggota_id kosong/tak ada, peran salah, sesi bukan milik/dibatalkan), 404 (jadwal/sesi/peserta), assign anggota tanpa akun dengan `wajib_absen:true` → tersimpan **false** & **tidak ada** notifikasi, duplikat single → 409, assign berakun sesi 2 → notifikasi `route=/jadwal/1/sesi/2` `reff=jadwal_sesi`, bulk `[3,4,6,4]` → `{ditugaskan:2, dilewati:1}` + 1 notifikasi (hanya yang berakun), `jml_ditugaskan` sesi = 3/4/3 tepat, list filter `sesi_id`/`wajib_absen`/`q`; respon oleh user lain & SA → 403, status salah → 422, tolak lalu terima → 200 (`direspon_pada` terisi), penugas menerima 2 notifikasi respon; unread Citra 1 / Dedi 1 / Mod 2, mark-read milik orang lain → 404, mark-read sendiri → 200 idempoten, `baca-semua` → `{ditandai:2}`, inbox SA 0, `user_id` tidak bocor; remove → 200, remove ulang → 404, re-assign → 201; `log_aktivitas` 7 baris; DB sekali-pakai di-drop. **Catatan commit:** perubahan pending di `slam-team-app/` + `workflow/prompts/04-*` (pekerjaan 15-lokasi APP yang belum selesai) sengaja **tidak** ikut — commit di-scope ke `slam-team-api/` seperti commit API sebelumnya.
- **2026-09-13** — #31 `16-jadwal` API selesai (Opus · xhigh) — commit `ed849de`. Modul `internal/modules/core/jadwal/` (domain/dto/repository/service/handler + `main.jadwal.go`, `Initialize(db, jwtMgr, permGuard, auditor)` seperti `lokasi`), didaftarkan di `router.go` setelah `lokasi`. Rute: `GET/POST /jadwal`, `GET/PUT/DELETE /jadwal/{id}`, `POST /jadwal/{id}/generate-sesi` (`jadwal.update`), `GET /jadwal/{id}/sesi` (`jadwal.read`), `PATCH /sesi/{id}/batalkan` (`jadwal.batal_sesi`); `jadwal.*` sudah ter-seed (diverifikasi, tidak di-seed ulang — catatan: seeder memberi `jadwal.delete` hanya ke Admin, Moderator 403, konsisten dengan pola "aksi berbahaya = Admin" di seeder walau tabel Bab 3.5 menulis Mod CRUD). Migrasi **`0015_jadwal`** persis `.dbml`: enum `pola_ulang`/`mode_absen`/`status_sesi` dirujuk dari `0001`, `hari_ulang int[]` (+CHECK `<@ {0..6}`), `status varchar(20)` + CHECK 4 nilai (bukan enum — DBML menang atas DB dev hasil ekspor yang memakai `status_jadwal`), CHECK `jam_selesai > jam_mulai`, partial-unique `kode WHERE is_deleted=false`, index `(tanggal_mulai,status)` + `lokasi_id`; `jadwal_sesi` `UNIQUE(jadwal_id,tanggal_lokal)`, CHECK `selesai_utc>mulai_utc` & `absen_tutup_utc>absen_buka_utc`, index `(tanggal_lokal,status)` + `(absen_buka_utc,absen_tutup_utc)`. FK **langsung**: `lokasi_id → mst_lokasi` RESTRICT, `inorga_id → mst_inorga` SET NULL, `jadwal_sesi.jadwal_id → jadwal` RESTRICT, audit `*_by → users`; **`kegiatan_id` FK ditunda** — kolom dibuat, seam `TODO(22-kegiatan)` di kaki `0015.up.sql` (+ di service, validasi eksistensi juga ditunda). `down` drop kedua tabel, enum tetap. Domain: `HariUlang domain.IntArray` (Scanner/Valuer teks `{1,3}` — pgx stdlib menyerahkan `int[]` sebagai string, tanpa dependensi `lib/pq`), `JamMulai/JamSelesai string` (`time` PG, disimpan `HH:MM:SS`), enum sebagai string type, proyeksi baca `lokasi_nama` (JOIN) + `jumlah_sesi` (subquery) bertag `->`. Service create/update: `kode` unik → 409, `jam_selesai>jam_mulai` (+format `HH:MM`/`HH:MM:SS`) → 422, `time.LoadLocation` → 422, `lokasi_id`/`inorga_id` tak ada → 422, `tanggal_selesai`/`tanggal_akhir_ulang` < `tanggal_mulai` → 422, `hari_ulang` wajib untuk `mingguan` (didedup+urut; dikosongkan untuk tidak_berulang/harian/bulanan), `is_berulang` diturunkan dari `pola_ulang`; **snapshot** `latitude/longitude/radius_meter/timezone` dari `mst_lokasi` bila kosong (nilai eksplisit menimpa). **generate-sesi** (`service/recurrence.go`, fungsi murni `Dates(rule, dari, sampai)` + unit test): harian tiap N hari berjangkar `tanggal_mulai`; mingguan hari di `hari_ulang` (0=Minggu) tiap N minggu (minggu mulai Senin); bulanan tanggal sama tiap N bulan, bulan pendek **dilewati** (bukan dipepetkan); **kustom** (tak didefinisikan DBML) = tiap N hari difilter `hari_ulang` bila ada; `tidak_berulang` → **tepat satu** sesi di `tanggal_mulai` (`hari_ulang`/`tanggal_akhir_ulang` diabaikan). Jendela = [`dari_tanggal`, min(`sampai_tanggal`, `tanggal_akhir_ulang`)], semua field body opsional (default dari jadwal: body kosong = materialisasi jadwal apa adanya; jadwal berulang tanpa `tanggal_akhir_ulang` wajib `sampai_tanggal`), cap 1000 sesi/panggilan → 422. `mulai_utc` = `time.Date(tanggal, jam, loc).UTC()` dengan **`loc = jadwal.timezone`**; `timezone` di body bila dikirim harus sama dengan timezone jadwal (beda → 422, sengaja ditolak, bukan diabaikan). `absen_buka_utc = mulai − buka`, `absen_tutup_utc = mulai + tutup` (default `buka_absen_mnt`/`tutup_absen_mnt`; keduanya 0 → 422). Insert batch (chunk 500, transaksi) `INSERT … ON CONFLICT (jadwal_id, tanggal_lokal) DO NOTHING RETURNING` → `{dibuat, dilewati, sesi[]}` (sesi = semua baris pada tanggal kandidat); jadwal `dibatalkan` → 409. `SesiResp` menambah `mulai_lokal`/`selesai_lokal` ("07:00" di zona sesi) agar kalender tak perlu konversi. **batalkan**: `alasan_batal` wajib (422), sesi sudah `dibatalkan`/`selesai`/`selesai_utc` sudah lewat → 409, `modified_by` terisi. Hapus jadwal = soft delete polos (sesi ikut tak terjangkau karena semua baca lewat induk non-deleted); seam `TODO(18-absensi)` untuk guard absensi. `log_aktivitas`: create/update/delete/generate_sesi/batal_sesi. List: filter `status`, `lokasi_id`, `pola_ulang`, `q`, rentang `tanggal_dari/sampai` (uji tumpang-tindih `tanggal_mulai ≤ sampai AND COALESCE(akhir_ulang, selesai, mulai) ≥ dari`), sort whitelist, `per_page` cap 100. **Seam 15-lokasi ditutup:** `CountJadwalByLokasi` (raw `SELECT count(*) FROM jadwal WHERE lokasi_id=? AND is_deleted=false`), `SoftDelete` lokasi terpakai → **409** "lokasi masih dipakai N jadwal", `jumlah_jadwal` di list+detail via subquery (`->`), semua `TODO(16-jadwal)` dihapus. **Bug ikutan diperbaiki (lintas modul):** `pkg/validator.Explain` menjanjikan kunci = path JSON tapi `Register()` tak pernah memasang `RegisterTagNameFunc`, sehingga error binding field multi-kata keluar sebagai `alasanbatal`/`tanggallahir`; kini tag `json` (fallback `form`) dipakai → `alasan_batal`. **Catatan:** `tanggal_selesai` tidak dipakai generator (hanya informasi/overlap list). **Verifikasi:** `go build ./... && go vet ./... && go test ./...` bersih (unit test generator: semua pola, cap, konversi WIB/WIT; `IntArray` round-trip); DB dev `slamteam_db` tetap v1 DIRTY (hasil ekspor `.dbml`, tak disentuh) → DB sekali-pakai `slamteam_migtest`: `migrate up` → v15 bersih, `down 1` → v14 (tabel hilang), `up` → v15, skema cocok DBML; seed + `create-superadmin` + Mod/User via SQL; **96/96** pemeriksaan HTTP lulus: validasi 422 (jam, tz, hari_ulang, lokasi/inorga, kode 409), snapshot lokasi, harian 01–10 Sep → 10 dibuat, **re-run 0 dibuat/10 dilewati**, 07:00 WIB → `00:00Z`, jendela −30/+15, mingguan Rab/Sab Sep 2026 → 9 tanggal tepat, bulanan tgl 31 lewati Feb/Apr/Jun, tanggal tunggal WIT → **tepat 1** sesi (re-run 1 dilewati), tz beda → 422, batalkan tanpa alasan → 422, sesi lewat → 409, User → 403 & Mod → 200, batal ganda → 409, User GET 200 / POST-PUT-DELETE-generate 403, hapus lokasi terpakai → 409 (`jumlah_jadwal`=4) lalu 200 setelah jadwal dihapus, `log_aktivitas` 21 baris; DB sekali-pakai di-drop.
- **2026-09-12** — #29 `15-lokasi` API selesai (Sonnet · high) — commit `9b8221d`. Modul `internal/modules/core/lokasi/` mengikuti layout `instansi` (domain/dto/repository/service/handler + `main.lokasi.go`). Migrasi `0014_mst_lokasi` persis `.dbml`: CHECK `latitude BETWEEN -90 AND 90` / `longitude BETWEEN -180 AND 180` / `radius_meter > 0`, partial-unique `kode WHERE is_deleted=false`, FK `foto_file_id → mst_file`, audit FK `created_by/modified_by/deleted_by → users` **langsung** (tabel `users` sudah ada). Service: `kode` unik → 409, `time.LoadLocation(timezone)` gagal → 422, foto (`foto_file_id`) divalidasi via `FileExists` (pola `instansi`, bukan `unit`), `log_aktivitas` create/update/delete. `RequirePermission("lokasi.<aksi>")`; `lokasi.*` sudah ter-seed (`seed_rbac.go` — tidak di-seed ulang). **Seam sengaja:** `jadwal` belum ada (16-jadwal) → `DELETE` soft-delete polos, `jumlah_jadwal` selalu 0, `TODO(16-jadwal)` di `service.SoftDelete` menandai guard 409 yang harus ditambahkan nanti. **Bug ikutan (bukan regresi dari kode ini, sudah ada di modul referensi `instansi`):** `baseQuery` via `model.NotDeleted` (`WHERE is_deleted = false` tanpa kualifikasi tabel) ambigu begitu di-JOIN ke `mst_file` (juga punya `is_deleted`) — `lokasi_repository.go` memakai kualifikasi eksplisit `mst_lokasi.is_deleted = false` (pola yang sudah dipakai modul `unit`), bukan scope bersama; `instansi`/`profile_club` masih membawa bug yang sama (lihat catatan bug #27) — tidak disentuh, di luar cakupan prompt ini. **Verifikasi:** `go build ./...` + `go vet ./...` bersih; DB dev lokal (`slamteam_db`) memang diimpor langsung dari `.dbml` dengan `schema_migrations` v1 DIRTY (catatan lama di #7) sehingga migrate tidak dijalankan di sana — dibuat DB sekali-pakai `slamteam_migtest`, `migrate up` → v14 bersih, `down 1` → v13, `up` → v14 lagi, skema cocok DBML persis; seed rbac+instansi, `create-superadmin`, lalu uji HTTP penuh: create/list/detail/update/delete 200, `kode` duplikat → 409, latitude di luar rentang → 422, `radius_meter=0` → 422, timezone tak valid → 422, get setelah delete → 404, `log_aktivitas` 3 baris (create/update/delete); role Moderator & User (dibuat manual via SQL karena modul `anggota`/`usermgmt` HTTP punya bug lain yang tak terkait) → GET 200, POST/DELETE 403; DB sekali-pakai di-drop setelahnya.
- **2026-09-12** — #7 `04-auth-session` API selesai (Opus · high) — commit `666da22`. Placeholder `auth` diganti tanpa membangun ulang: `domain.User` kini memetakan `users` **persis** `.dbml` (`anggota_id *int64`, `username`, `email`, `password json:"-"`, `role_id`, `timezone`, `is_aktif`, `login_terakhir`, `password_diubah`, `gagal_login`, `terkunci_sampai`, embed `model.Audit`; **tidak ada kolom `name`** — `nama` di-join dari `anggota.nama_lengkap`, `foto_uuid` dari `mst_file` via `foto_profil_file_id`) + `domain.SesiLogin`. Migrasi **`0013_sesi_login`** (FK `user_id → users` **CASCADE**, `refresh_token` UNIQUE, index `user_id`; `users` tidak disentuh; `down` hanya drop `sesi_login`). Repository: `FindByUsernameOrEmail` satu query `(username=? OR email=?) AND is_deleted=false`, `TouchLogin`/`IncrementGagalLogin` (RETURNING counter)/`ResetGagalLogin`/`LockUntil`, `CreateSesi`/`FindActiveSesiByToken` (belum dicabut & `berlaku_sampai > now()`)/`RevokeSesi`/`RevokeAllSesi`. Service: login → terkunci ⇒ 403 "akun terkunci, coba lagi dalam …", nonaktif ⇒ 403, password salah ⇒ **401 generik** ("username/email atau password salah") + counter; **5 gagal ⇒ `terkunci_sampai = now+15m`** (konstanta, belum dari `mst_pengaturan`); sukses ⇒ reset counter + `login_terakhir`, access JWT (klaim `user_id`, **`anggota_id`**, `role_id`, `role_level`, `is_super`, `perm_version` — peran utama dari `user_role` via hakakses, fallback `users.role_id`), refresh **opaque `crypto/rand` 32B base64url** disimpan di `sesi_login` (`berlaku_sampai = now + JWT_REFRESH_TTL_HOURS`, ip/user_agent), `audit.Write(auth/login)`. Refresh: cari sesi aktif → **revoke dulu baru terbitkan** (rotasi; replay token lama ⇒ 401), akun nonaktif/terkunci saat refresh ⇒ sesi dicabut + 401. Logout (bearer): body `{refresh_token}`, sesi milik user lain ⇒ 403, sesi sudah mati ⇒ tetap 200; audit `auth/logout`. Rute: `POST /auth/login`, `POST /auth/refresh` (publik), `POST /auth/logout`, `GET /auth/me`, `GET /auth/me/permissions` (bearer) — path `/auth/me*` dipertahankan karena APP (#8) sudah memakainya. `/me` = user + `role{id,nama,level,is_super}` + `anggota{id,nama_lengkap,no_induk,foto_uuid,foto_url}`; `/me/permissions` tak berubah. `pkg/jwt`: `Claims.AnggotaID *int64` (`anggota_id,omitempty`), `IssueAccess(...)` baru, `IssueWithRole`/`Issue` tetap sebagai wrapper (kompatibel), `Manager.TTL()` untuk `expires_in`. `auth.Initialize` kini menerima `auditor` + `cfg.JWT.RefreshTTL`. **Bug ikutan diperbaiki:** `hakakses.GetPermVersion` men-scan ke field **unexported** sehingga GORM tidak mengisinya — `perm_version` selalu 0 di semua token; kini terbaca (`1` setelah seed). Dokumen: CLAUDE.md gotcha "auth placeholder" diganti catatan `users` tanpa kolom nama + refresh opaque; README tabel endpoint + bootstrap `create-superadmin` (bagian `CREATE TABLE users` mainan dihapus). **Verifikasi** (DB bersih `slamteam_verify`, lalu di-drop): `go build ./... && go vet ./... && go test ./...` lolos (+ test `IssueAccess` round-trip `anggota_id`); `migrate up` → v13, `down 1` → v12 (`users` 19 kolom utuh), `up` → v13; `seed` + `create-superadmin`; login **username** dan **email** 200; JWT terdekode memuat `user_id:1, anggota_id:1, role_id:1, is_super:true, perm_version:1`; baris `sesi_login` + `log_aktivitas(auth/login)` masuk; `/me` & `/me/permissions` 200; refresh ⇒ token baru, replay lama ⇒ 401; logout 200 ⇒ refresh berikutnya 401 (`dicabut_pada` terisi); identifier tak dikenal ⇒ 401 generik; password `<8` ⇒ 422 `{password:"minimal 8"}`; 5× salah ⇒ `gagal_login=5`, `terkunci_sampai=+15m`, password benar ⇒ 403; setelah kunci lewat ⇒ 200 dan counter 0/NULL; tidak ada `password` di respons mana pun. **Catatan:** DB dev lokal `slamteam_db` ternyata diimpor langsung dari `.dbml` (sudah punya `sesi_login`) dan `schema_migrations` berstatus **v1 DIRTY** — `slamctl migrate up` di sana akan menolak; perlu `force`/reset manual atau pakai DB baru dari migrasi. Ambang lockout masih konstanta (`maxGagalLogin=5`, `lockDuration=15m`), belum kunci `mst_pengaturan`.
- **2026-09-12** — #8 `04-auth-session` APP selesai (Opus · high) — commit `2a544cf`. UI auth **diperluas, bukan dibangun ulang**; `PermissionService`, ketiga guard, dan `HasPermissionDirective` (dari 05) dipakai apa adanya. `User` kini = `dto.AuthUser` (`username`, `nama`, `anggota_id`, `role{id,nama,level,is_super}`, `perm_version`) — konsumen `u.name` (topbar, dashboard) diganti `u.nama`; `AuthResponse` = `{access_token, refresh_token, token_type, expires_in, user}`. `AuthService.login(identifier, password)` menyimpan `slam_token` + `slam_refresh` + `slam_user` lalu memanggil `PermissionService.load()` **sebelum** emit, sehingga sidebar dan `*hasPermission` sudah benar di paint pertama dashboard; tidak perlu round-trip `/auth/me` kedua karena payload login sudah memuat user + role. `refresh()` merotasi pasangan token, gagal ⇒ `clearSession()` + lempar; pemanggil bersamaan **berbagi satu request** (`share()` + field in-flight) karena refresh token rotasi bersifat sekali pakai — dua 401 serentak yang memicu dua refresh akan menendang pengguna keluar. `logout()` POST `/auth/logout {refresh_token}` best-effort (di-subscribe **sebelum** storage dibersihkan supaya `authInterceptor` masih membaca Bearer), lalu clear + `perms.clear()` + navigate `/auth/login` — topbar tinggal memanggilnya. `hydrate()` di `provideAppInitializer` (fire-and-forget: paint pertama tidak menunggu API): token yang selamat dari reload diverifikasi ke `GET /auth/me` (diproyeksikan ke bentuk `AuthUser`, `foto_url` diambil dari `anggota`) + izin dimuat; 401 di sini mengikuti jalur refresh → logout biasa. `errorInterceptor`: 401 di luar `/auth/login|refresh|logout` ⇒ **satu** refresh senyap lalu request asli diulang dengan Bearer baru (diset manual — `next` di interceptor kedua tidak melewati `authInterceptor` lagi); refresh gagal ⇒ logout + redirect + toast `SESSION_EXPIRED`, dan pemanggil tetap menerima **401 asli**, bukan error refresh. 403 ⇒ toast (tak berubah) kecuali dari `/auth/login` (akun terkunci ditampilkan inline oleh form, bukan sebagai penolakan RBAC). Login: kontrol `email` → `identifier` (tanpa `Validators.email`), password `min 8` mengikuti DTO, `saving()` men-disable submit, 401 ⇒ `AUTH.LOGIN.ERROR_INVALID` (generik — server sengaja tidak membocorkan bagian mana yang salah), 403 ⇒ `AUTH.LOGIN.ERROR_LOCKED`. Namespace `LOGIN.*` dipensiunkan → `AUTH.LOGIN.*`; IND & ENG sinkron (443 kunci). Desain: halaman login yang ada sudah patuh token (dark, bloom merah, eyebrow, alert ber-glyph) — hanya field yang diganti, plus hint dim "username atau email". **Verifikasi:** `npm run build:local` lolos; `npm test` **44/44** — `auth.service` (10: login username & email, rotasi, refresh bersamaan → 1 request, refresh gagal → clear, logout dengan Bearer lalu clear, hydrate), `error.interceptor` (7: refresh sekali + replay Bearer baru, 2 request 401 serentak → 1 refresh & keduanya pulih, refresh gagal → logout+redirect+401 asli, `/auth/login` 401/403 lolos tanpa refresh/toast), `login` (6). **Catatan:** handler `/auth/refresh` & `/auth/logout` belum ada di API yang ter-commit (baris #7 masih ⬜; DTO-nya sudah di working tree, handler baru punya Login/Me/MePermissions) — uji end-to-end login/refresh/logout melawan API sungguhan menunggu #7 selesai. Kontrak yang dipakai persis `auth.dto.go` di working tree. Duplikasi kecil yang disengaja: saat reload di rute ber-`permissionGuard`, `hydrate()` dan `ensureLoaded()` sama-sama memanggil `/auth/me/permissions` — idempoten, `PermissionService` tidak disentuh sesuai instruksi.
- **2026-09-11** — #16 `07-user-management` APP selesai (Opus · high) — commit `98b108f`. 3 surfaces (admin/moderator/user) dengan komponen shared: `UserListComponent` (signal-based list, search, status filter, pagination, skeleton loading, avatar, role/level display, active/inactive badge, lock indicator, action buttons gated by permission), `UserFormComponent` (reactive form, anggota picker dari `/anggota-tersedia`, role select dengan anti-escalation UX — role level >= actor level disabled, password optional on edit, active toggle, buka kunci toggle on edit). Routes 3 permukaan `canActivate:[permissionGuard]` dengan `data:{permission, base}`. i18n `USERMGMT.*` IND+ENG + tambahan `COMMON.ACTIVE/INACTIVE/CREATE`. Verifikasi: `ng build` bersih (0 errors) + browser validation di 3 surfaces (list + create form).
- **2026-09-11** — #27 `14-profile-club` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0012_profile_club`, singleton guard (`CountActive > 0 → 409`), 6 endpoints (list/detail/create/update/delete + public profil), 3 file refs (banner, logo_simple, logo_besar), public endpoint tanpa auth. Bugs fixed: `toResp` pointer mismatch, `FileExistChecker` interface naming, `.Omit()` computed columns, qualified `is_deleted` ambiguitas. Verifikasi: `go build`/`go vet` + 6 endpoint tests bersih.
- **2026-09-11** — #28 `14-profile-club` APP selesai (Sonnet · high) — commit `273d305`. Singleton form (auto-detect create vs edit via GET list), 3 image uploads dengan blob preview + `revokeObjectURL`, reactive form dengan validasi, i18n `PROFILE_CLUB.*` IND+ENG. Verifikasi: `ng build` bersih + browser validation.
- **2026-09-11** — #25 `13-dokumen` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0011_mst_dokumen`, CRUD, file upload (kategori=dokumen), download endpoint. Verifikasi: `go build`/`go vet` bersih.
- **2026-09-11** — #26 `13-dokumen` APP selesai (Sonnet · high) — commit `273d305`. List + form + file upload + download, reff_type filter, i18n `DOKUMEN.*` IND+ENG. Verifikasi: `ng build` bersih.
- **2026-09-11** — #23 `12-medsos` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0010_mst_medsos`, CRUD, anggota FK, kode unik per anggota, audit log. Verifikasi: `go build`/`go vet` bersih.
- **2026-09-11** — #24 `12-medsos` APP selesai (Sonnet · high) — commit `273d305`. List + form, jenis filter (instagram/youtube/tiktok/dll), anggota picker, i18n `MEDSOS.*` IND+ENG. Verifikasi: `ng build` bersih.
- **2026-09-11** — #21 `11-inorga` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0009_inorga`, CRUD, logo/banner/SK upload via file-management, status toggle. Verifikasi: `go build`/`go vet` bersih.
- **2026-09-11** — #22 `11-inorga` APP selesai (Sonnet · high) — commit `273d305`. List + form (CKEditor-style textarea untuk deskripsi), 3 file uploads (logo, banner, SK), i18n `INORGA.*` IND+ENG. Verifikasi: `ng build` bersih.
- **2026-09-11** — #19 `10-prestasi` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0008_prestasi`, 6 CRUD endpoints, tingkat enum (kabupaten/provinsi/nasional/internasional), anggota FK, audit log. Verifikasi: `go build`/`go vet` bersih.
- **2026-09-11** — #20 `10-prestasi` APP selesai (Sonnet · high) — commit `273d305`. List + form, tingkat filter, foto/flyer upload, i18n `PRESTASI.*` IND+ENG. Verifikasi: `ng build` bersih.
- **2026-09-10** — #15 `07-user-management` API selesai (Opus · high) — commit `273d305`. Anti-eskalasi (user tidak bisa naikkan role di atas level sendiri), 7 endpoints (list/create/read/update/delete + role-change + reset-password), dual-band handler (superadmin vs admin). Verifikasi: `go build`/`go vet` bersih.
- **2026-09-10** — #17 `09-unit` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0007_unit`, 6 endpoints CRUD, approval flow via PUT (setuju/tolak), scope enforcement (admin instansi hanya lihat unit instansinya), anggota tersedia (list anggota belum punya unit), foto UUID→file_id resolve. Verifikasi: `go build`/`go vet` bersih.
- **2026-09-10** — #18 `09-unit` APP selesai (Sonnet · high) — commit `273d305`. Unit-list (signal-based, filter, pagination), unit-form (anggota picker, spec fields, foto upload, approval toggle), routing, i18n `UNIT.*` IND+ENG. Verifikasi: `ng build` bersih.
- **2026-09-10** — #13 `06-anggota` API selesai (Sonnet · high) — commit `273d305`. Migrasi `0006_anggota` (buat `mst_wilayah` + `anggota` table 22 kolom, indeks unik `no_induk`, FK 5 tabel). CRUD: list (search ILIKE nama/NRA + filter instansi/jenis/status + sort whitelist), create, read, update, delete (soft). `no_induk` absen dari DTO (read-only, diisi modul KTA). Audit log `nilai_lama`/`nilai_baru`. Guard soft-delete: active KTA + user account existence. Permission seed idempotent (anggota CRUD 4 izin). Verifikasi: `go build`/`go vet` bersih.
- **2026-09-10** — #14 `06-anggota` APP selesai (Sonnet · high) — commit `273d305`. `AnggotaList` (signal-based, search, jenis/status filter, pagination, skeleton, `*hasPermission` gates), `AnggotaFormComponent` (reactive form, instansi dropdown, 3 file upload via FileService + preview, foto blob revoke), `AnggotaDetail` (profile card, biodata grid, private image blob load). Routes `/anggota` (list/create/:id/edit/:id). i18n `ANGGOTA.*` IND+ENG. Verifikasi: `ng build` bersih (3 lazy chunks).
- **2026-09-10** — **UI Revamp** (Taste Skill integration) — commit `6d46005`. 13 Taste Skills diinstal, `SlamIcon` component (inline SVG, 30+ icons), semua emoji diganti (sidebar, topbar, role-list, instansi-list), `.slam-heading`/`.slam-card` diglobalisasi, skeleton loaders, empty states, micro-interactions (hover/active), inline styles dipindah ke CSS classes.
- **2026-09-10** — #11 `08-instansi` API selesai (Sonnet · high) — commit `6d46005`. Migrasi `0005_mst_instansi` (22 kolom, FK ke `mst_file` via `logo_utama_uuid`/`logo_club_uuid`). CRUD: list (paginasi + search + filter status), create, read, update, delete (soft). Logo upload reuses file-management module. Verifikasi: `go build`/`go vet`/`go test` bersih.
- **2026-09-10** — #12 `08-instansi` APP selesai (Sonnet · high) — commit `6d46005`. Instansi list (tabel + paginasi + search + status filter + logo lazy-load via mouseenter), instansi form (create/edit + dual logo upload + preview). `InstansiService`, `Instansi` model, i18n `INSTANSI.*`. Verifikasi: `npx ng build` bersih.
- **2026-09-10** — #9 `05-hak-akses` API selesai (Opus · xhigh) — commit `6d46005`. RBAC penuh: role CRUD (4 endpoint), permission matrix read/write, `middleware.RequirePermission` baru yang query `role_permissions` + `role_modul_permissions`, `slamctl seed-rbac` (55 izin per 19 modul + 3 file permissions, idempoten). Migrasi `0004_rbac` (3 tabel: `roles`, `role_permissions`, `role_modul_permissions`). Auth service diperbarui: `LoginResponse` + `GET /auth/me` kini terbitkan `role_id`, `role_level`, `is_super` + daftar permission. Verifikasi: `go build`/`go vet`/`go test` bersih.
- **2026-09-10** — #10 `05-hak-akses` APP selesai (Opus · xhigh) — commit `6d46005`. Role list (tabel + aksi), role form (create/edit), role matrix (permission grid), `adminGuard` (gates pengaturan), `PermissionService` (load + cache permissions dari `/auth/me`), `HasPermissionDirective` (structural directive `[hasPermission]`). I18n `HAK_AKSES.*` + `MENU.*` ditambah. Verifikasi: `npx ng build` bersih.
- **2026-09-09** — #5 `02-file-management` API selesai (Opus · high) — commit `01d98d9`. Migrasi `0003` identik `.dbml`; `imaging` (murni Go, WebP ditunda → JPEG); varian px dari `mst_pengaturan`; serve ber-izin via `JWTOptional`; seam `perm.Require` siap untuk 05.
- **2026-09-09** — #6 `02-file-management` APP selesai (Opus · high) — commit `e0a52a5`. `FileService` di atas `ApiService`; `<app-file-upload>` (dropzone → emit `{id,uuid}`) + `<app-secure-image>` (blob privat, revoke object URL); `UploadResp` belum punya `id` ⇒ `FileRef.id` masih undefined.
- **2026-09-09** — #4 `03-pengaturan-log` APP selesai (Sonnet · high) — commit `62f1a0d`. Halaman self-generating per `tipe_nilai`, `adminGuard` meniru `RequireAdmin` (belum ada `pengaturan.*`), `User.role` ditambah (opsional — auth Fase 0 belum menerbitkan klaim peran).
- **2026-09-09** — #3 `03-pengaturan-log` API selesai (Sonnet · high) — commit `c90d083`. Gate Fase 0 memakai `middleware.RequireAdmin` (peran), bukan `pengaturan.read/update`: tidak ada izin `pengaturan.*` yang di-seed karena pengaturan bukan salah satu dari 19 `mst_modul`. FK `modified_by`/`aktor_user_id` → `users.id` ditunda ke migrasi yang membuat tabel `users`.
- **2026-09-09** — #1 `01-foundation` API selesai (Opus · high) — commit `46e69f6`. Envelope response (`apperr` + `FromError`), `model.Audit`, `pagination.Paginated[T]`, `tzdata`, CORS config, `slamctl` + migrator.
- **2026-09-09** — #2 `01-foundation` APP selesai (Opus · high) — commit `46e69f6` (satu commit bersama API). `ApiService` + `Page<T>`, interceptor 401/403, design tokens (dark + merah), i18n `COMMON.*`/`VALIDATION.*`.
- **2026-09-09** — #5 `02-file-management` API selesai (Opus · high) — commit `01d98d9`. Migrasi `0003_file_layer` diverifikasi byte-per-byte terhadap skema `.dbml` yang sudah ada di DB lokal (kolom, tipe, default, indeks, FK cascade — diff kosong); FK audit → `users.id` ditunda seperti di `0002`. Pustaka gambar **`disintegration/imaging`** (murni Go, tanpa cgo) + `rwcarlsen/goexif` untuk `metadata_exif`; **WebP ditunda sadar** → varian `medium`/`low` JPEG q80/q70, kecuali sumber ber-transparansi (PNG/GIF) yang tetap PNG agar logo tidak berlatar hitam. Ukuran piksel dibaca dari `mst_pengaturan` (diuji: setelan diubah → varian ikut) dan gambar tidak pernah diperbesar. `variants[]` berisi objek (doc modul §3), bukan string (`api-endpoints.md` §10). Ditambah `GET /files/{uuid}` untuk polling `status_proses` (opsional menurut doc §3). Route stream memakai `middleware.JWTOptional` baru supaya logo publik bisa dimuat tanpa token sementara berkas privat tetap 401/403. Gerbang Fase 0: `JWTAuth` saja + kepemilikan di service; seam `perm.Require("file.create"/"file.delete")` ditandai di `main.file.go` dan `service.authorizeRead` untuk disambung di `05-hak-akses` — **seeder RBAC di 05 wajib membuat kedua izin itu** walau `file` bukan salah satu dari 19 `mst_modul`. Config baru `STORAGE_ROOT` (root tak bisa ditulis ⇒ gagal saat boot, `router.Setup` kini mengembalikan error). Verifikasi: `go build`/`go vet`/`go test` bersih; `migrate up → down 1 → up → down all` bersih; 55 pemeriksaan end-to-end lulus (gambar ber-EXIF Orientation 6 tersimpan tegak 100×200 → 200×100, 1 `mst_file` + 3 `mst_file_varian`, PDF hanya `original`, Range 206, dedup, 403 lintas pemilik, 401 tanpa token, soft delete + `log_aktivitas`). Uji dijalankan di DB bersih `slamteam_migtest` karena `slamteam_db` lokal berisi skema hasil ekspor dbdiagram yang dimuat manual dan `schema_migrations`-nya tersangkut `version=1, dirty=true`.
- **2026-09-09** — #6 `02-file-management` APP selesai (Opus · high) — commit `e0a52a5`. Lapisan berkas di frontend adalah **service bersama + 2 komponen reusable**, bukan halaman menu: tidak ada rute, tidak ada item sidebar, tidak ada guard (izin `file.create` dimiliki semua peran berakun). `FileService` dibangun **di atas `ApiService`** (modul 01), bukan `HttpClient` langsung, supaya base URL, envelope `{success,message,data,errors}` dan Bearer dari `authInterceptor` tetap di satu tempat — `ApiService.upload()`/`.blob()` sudah menyediakan persis yang dibutuhkan. `FormData` dikirim **tanpa `Content-Type` manual** (Bab 6.6) dan `extra` kosong dibuang agar `reff_id` yang absen tidak sampai ke server sebagai string `"null"`. `<app-secure-image>` mengambil varian sebagai **blob** lalu mem-bind object URL, dan **me-revoke** saat `uuid`/`varian` berubah maupun saat destroy (URL lama disimpan di field biasa, bukan signal, supaya `effect` tidak bergantung pada keluarannya sendiri); permintaan in-flight dibatalkan lewat `onCleanup` agar scroll cepat tidak menimpa gambar baru dengan blob lama. `<app-file-upload>` menampilkan pratinjau lokal (`URL.createObjectURL` dari `File`) selama unggah, lalu menyerahkannya ke varian `low` dari server, dan meng-emit `{id, uuid}`; input `value` (uuid) memuat berkas tersimpan lewat `GET /files/{uuid}` supaya form **edit** terbuka sudah terisi. **Seam:** `dto.UploadResp` Fase 0 hanya mengembalikan `uuid`, tidak `id`, padahal kolom pemilik bertipe `*_file_id bigint` — `UploadedFile.id`/`FileRef.id` dibuat opsional dan ditandai di `file.model.ts`; modul pemilik pertama yang butuh menulis `*_file_id` harus menambah `ID` ke `UploadResp` di API (satu baris di `service.respond`), tanpa perubahan di frontend. `fileErrorText` menangani dua bentuk kegagalan sekaligus — `HttpErrorResponse` (non-2xx) dan envelope mentah yang dilempar `ApiService.unwrap` untuk `success:false` ber-HTTP-200 — dan **menolak** memakai `message` dari `Error` transport biasa supaya kalimat teknis tidak bocor ke pengguna. Desain per `design-tokens.md`: dropzone `--slam-surface-2` + border putus-putus `--slam-border` → `--slam-primary` saat hover/drag, cincin fokus merah dipindahkan ke `<label>` karena `<input type=file>`-nya `visually-hidden` (tetap fokusabel, bukan `display:none`), status selalu berpasangan glyph (⚠/⏳) bukan warna saja, target sentuh 44px ikut media query global. i18n `FILE.*` + `COMMON.CHOOSE_FILE`/`REMOVE`, IND & ENG diverifikasi sinkron (112 kunci). Verifikasi: `npm run build:local` lolos; `npm test` **21/21** lolos — termasuk bukti revoke object URL saat ganti uuid & saat destroy (tidak ada blob menggantung), `FormData` tanpa `Content-Type`, tolak >15 MB dan tipe salah tanpa request, soft delete + emit `cleared`, dan pemuatan uuid tersimpan. Progress unggah masih **indeterminate** (ditandai `ponytail:`): `ApiService` mengembalikan body yang sudah di-parse, bukan aliran `HttpEvent`; ganti ke `HttpRequest` + `reportProgress` bila dokumen besar kelak butuh persentase.
