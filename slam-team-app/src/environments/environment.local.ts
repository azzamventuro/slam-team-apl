// Local development environment. Points at slam-team-api on :8080.
// Selected by the `local` build/serve configuration (see angular.json).
export const environment = {
  production: false,
  apiURL: 'http://localhost:8080/api/v1',
  appName: 'Slam Team (local)',
  defaultLang: 'IND', // IND | ENG
};
