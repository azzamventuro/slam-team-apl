// Default (production) environment. `ng build` uses this file.
// For local dev against slam-team-api, run `npm run serve:local`
// (the `local` configuration swaps in environment.local.ts).
export const environment = {
  production: true,
  apiURL: '/api/v1',
  appName: 'Slam Team',
  defaultLang: 'IND', // IND | ENG
};
